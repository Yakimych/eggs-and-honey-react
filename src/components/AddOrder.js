import React from 'react';

let AddOrder = (props) => {
  return (
    <div>
      <input type="text" />
      <input type="submit" value="Add" />
    </div>
  );
};

export default AddOrder;